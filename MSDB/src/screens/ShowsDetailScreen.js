import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ShowsDetailScreen = () => {
    return (
        <View>
            <Text>ShowsDetailScreen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default ShowsDetailScreen;